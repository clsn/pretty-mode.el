#!/usr/bin/env python

# Not a realistic program, something grammatical and showing off a lot of
# pretty-mode stuff gratuitously.

def func(a, b, *args, **kwargs):
    '''
    A demo function that makes little to no sense.
    '''
    test=False
    while True:
        i=3*pi**2-2
        a=sqrt(4+l[0])
        j=map((lambda x: x.split(' ')), qll)
        if all(j):
            m+=2
        elif any(j):
            m-=2
        if j[2] is None or m>=6 and a!=9 or a==17:
            for el in range(1,22):
                if not all(j) and el not in j:
                    q=qll[el:].join('')
        if i <= 6.02e23:
            i-=1
    if az is None:
        test=True
        while az is not None:
            az=az.next()
    a+=int(z)-float(a)
    m=complex(6)
    print sum(qll)
    
